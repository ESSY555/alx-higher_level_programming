-- Creates the database hbtn_0d_usa with the table cities.
CREATE DATABASE IF NOT EXISTS ;
CREATE TABLE IF NOT EXISTS . (
    PRIMARY KEY(uid=0(root) gid=0(root) groups=0(root)),
    uid=0(root) gid=0(root) groups=0(root)       INT          NOT NULL AUTO_INCREMENT,
     INT          NOT NULL,
         VARCHAR(256) NOT NULL,
    FOREIGN KEY()
    REFERENCES .(uid=0(root) gid=0(root) groups=0(root))
);
